<section class="user_info">
  <h1>
    <%= gravatar_for @user %>
    <%= @user.name %>
  </h1>
  <p>Location: <%= @user.location.present? ? @user.location : "-----" %></p>
</section>
<p><%= link_to 'profile', @user %> | 
<%= link_to 'followings', followings_user_path(@user) %> | 
<%= link_to 'followers', followers_user_path(@user) %></p>